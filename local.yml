- hosts: localhost
  become: true
  connection: local

  pre_tasks:
    - name: Update_repositories
      package:
        update_cache: yes
        changed_when: false

  tasks:
    - name: Install packages
      package:
        name: htop
